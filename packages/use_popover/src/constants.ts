import { ArrowRotations, SharedStyle } from './types';

export const ARROW_ROTATIONS: ArrowRotations = {
  top: 180,
  left: 90,
  right: -90,
  bottom: 0,
};

export const SHARED_STYLE: SharedStyle = {
  position: 'absolute',
  visibility: 'visible',
};
