import { Side, Overflowing } from './types';

export const DEFAULT_SIDES: Side[] = ['top', 'bottom', 'left', 'right'];

export const DEFAULT_OVERFLOWING: Overflowing = {
  top: false,
  left: false,
  bottom: false,
  right: false,
};
